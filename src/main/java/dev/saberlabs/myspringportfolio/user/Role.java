package dev.saberlabs.myspringportfolio.user;

/*
 * Enum representing the authorization roles available in the application.
 * - USER: Standard user with access to portfolio, fund, and investment features.
 * - ADMIN: Administrative user with elevated access (currently unused in the UI).
 * */
public enum Role {
    USER,
    ADMIN
}
